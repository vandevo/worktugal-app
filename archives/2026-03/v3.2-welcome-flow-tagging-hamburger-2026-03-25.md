# Thread Handover — v3.2-welcome-flow-tagging-hamburger-2026-03-25

---

## 1. Thread Overview

This session completed the new user onboarding automation for app.worktugal.com and closed three open infrastructure gaps: new accounts not appearing in Listmonk, no auth provider tagging for subscriber segmentation, and blog guides invisible on mobile. All work is deployed and live on the production Supabase project (jbmfneyofhqlwnnfuqbd) and Cloudflare Pages.

The session continued from a prior thread that had shipped the welcome-new-user edge function (email + Telegram ping) but had not yet subscribed new accounts to any Listmonk list. The user confirmed the gap via a screenshot showing a test Gmail account not appearing in Listmonk after signup. That gap was the primary driver of this session.

The strategic outcome is that every new app signup is now fully tracked: welcome email delivered, Telegram ops ping fired, subscriber added to Worktugal Main (list 3) with source, auth_provider, and signup_date attribs. The subscriber data is now rich enough to segment campaigns by signup era (legacy 15k vs app era) and by signup method (Google OAuth vs email).

---

## 2. Key Decisions Made

Technical, stack, automation, schema

Decision: Add Listmonk subscription to welcome-new-user edge function using session-based auth.
Impact: Every new app account is now in Listmonk list 3 (Worktugal Main) immediately on signup.
Files: supabase/functions/welcome-new-user/index.ts

Decision: Pass raw_app_meta_data from Postgres trigger to capture auth provider.
Impact: Supabase stores provider (google, email) in app metadata — previously the trigger only sent raw_user_meta_data. Now edge function extracts authProvider and writes it to Listmonk attribs.
Files: Postgres function public.handle_new_user_welcome() updated via migration update_welcome_trigger_add_app_meta.

Decision: Write structured attribs to Listmonk on every new app signup.
Impact: Each subscriber record now carries source: 'app_signup', auth_provider: 'google'|'email', signup_date: 'YYYY-MM-DD'. The legacy 15k list has no source attrib — clean separation between old and new era subscribers.
Files: supabase/functions/welcome-new-user/index.ts

Decision: Normalize all March 25 changelog entries to v3.2.
Impact: Version sequence is now clean: v3.0 → v3.1 → v3.2. Previously had v3.2, v3.3, v3.4, v3.5 all on the same date.
Files: project_changelog table (Supabase), src/components/Changelog.tsx FALLBACK_ENTRIES.

Decision: Unhide hamburger menu on mobile (Layout.tsx).
Impact: Mobile users can now reach Blog, Changelog, Community from the top nav. Previously the button had class "hidden" instead of "md:hidden" so it never rendered despite the full menu being wired up.
Files: src/components/Layout.tsx line ~206.

Decision: Blog Subscribers list in Listmonk set to Private.
Impact: List no longer appears on Listmonk self-service subscription page (if public URL is found). All subscriptions are programmatic — no reason for a list to be public.
Action: Manual — user must update via Listmonk admin UI.

Strategic, ICP, monetization, positioning

Decision: Surface blog guides on mobile via hamburger rather than replacing bottom nav COMMUNITY tab or adding a homepage section.
Impact: COMMUNITY (Telegram) stays in primary mobile nav. Blog is reachable from hamburger. Bottom nav slots preserved for highest-value actions.
Proof: Stitch design confirmed hamburger in mobile top nav area. Button was already built, just hidden.

Deferred or pending

Item: Set Blog Subscribers Listmonk list from Public to Private.
Blocker: Manual action in Listmonk admin UI — not scriptable.
Owner: Van.

Item: Backfill existing app accounts into Listmonk list 3.
Blocker: No bulk backfill was run this session. Users who signed up before today are not in list 3.
Owner: Van — run SQL bulk insert or use Listmonk API once list is confirmed Private.

Item: Verify ghost-member-webhook fires correctly post-confirmation.
Blocker: Ghost fires member.added after email confirmation click, not on initial subscription. Needs live test with a real non-existing email.
Owner: Van.

---

## 3. Assets Created

Code, SQL, scripts

Filename: supabase/functions/welcome-new-user/index.ts
Stack: Deno / Supabase edge function
Changes: Added Listmonk session-based auth block (login form → cookie → POST /api/subscribers to list 3). Added auth provider extraction from raw_app_meta_data. Added source, auth_provider, signup_date to Listmonk attribs. Updated Telegram message to show Provider field.

Filename: Postgres migration update_welcome_trigger_add_app_meta
Stack: SQL / pg_net
Changes: Updated handle_new_user_welcome() trigger function to include raw_app_meta_data in the JSON body sent to the welcome-new-user edge function.

Automations

Edge function: welcome-new-user (deployed, Supabase project jbmfneyofhqlwnnfuqbd)
Trigger: AFTER INSERT on auth.users via Postgres trigger on_new_user_welcome.
Action: Sends welcome email via Resend, pings Telegram ops (@WorktugalPassBot), adds to Listmonk list 3 with full attribs.

Edge function: ghost-member-webhook (deployed prior session, confirmed live)
Trigger: Ghost member.added webhook → POST to Supabase edge function.
Action: Adds subscriber to Listmonk list 5 (Blog Subscribers), pings Telegram ops.

Other

Changelog DB entries: 5 new rows inserted into project_changelog table (all is_public: true, all version v3.2, date 2026-03-25). Titles: Welcome email on signup, Compliance guides, New accounts redirect to diagnostic, Guides shortcut on dashboard, Worktugal on Telegram.

---

## 4. Pending or Next Steps

Ops or system builds

Action: Set Listmonk Blog Subscribers list (list 5) from Public to Private.
Owner: Van.
Dependency: Listmonk admin UI — manual.
Check: mail.worktugal.com/admin/lists — edit list 5, set type to Private.

Action: Backfill existing app accounts into Listmonk list 3.
Owner: Van or Claude Code next session.
Dependency: Run SQL query against auth.users to extract emails, then batch POST to Listmonk API.
Check: Subscriber count in list 3 should match or exceed total auth.users count.

Action: Test ghost-member-webhook with a fresh email address.
Owner: Van.
Dependency: Subscribe on blog.worktugal.com with a new email, confirm, check Listmonk list 5 + Telegram.
Check: Subscriber appears in list 5 with source: ghost_blog attrib.

Growth or content

Action: Post NISS article to r/PortugalExpats.
Channel: Reddit — r/PortugalExpats.
CTA: Free diagnostic link.
Distribution path: Run /content write reddit NISS post, post 9am-3pm Lisbon time.

Action: Post NISS article to @worktugal Telegram channel.
Channel: Telegram public channel.
CTA: blog.worktugal.com/niss link.
Distribution path: Manual post or via post-to-channel edge function.

Action: Update send-diagnostic-email to link NISS guide when NISS gap detected in trap_flags.
Channel: Diagnostic email.
CTA: blog.worktugal.com/niss.
Distribution path: Edit supabase/functions/send-diagnostic-email/index.ts — conditional HTML block when trap id includes niss.

---

## 5. Suggested Next Thread Name

Proposed name: v3.3-content-distribution-2026-03-25
Rationale: Ops automation is complete. Next session should focus on distributing the NISS article (Reddit, Telegram), updating diagnostic email to link the guide on NISS trap, and potentially writing the next compliance guide.

---

## Audit Checks

Indexing: blog.worktugal.com NISS article is live and published. No sitemap changes this session.
Schema: Postgres trigger updated (handle_new_user_welcome). No new tables.
Performance: No performance changes.
Links: Hamburger nav links Blog (blog.worktugal.com), Changelog (/changelog), Community (t.me/worktugal) — all verified in NAV_LINKS.
Seven day metrics: Check Listmonk list 3 subscriber growth rate. Check welcome email open rate in Resend dashboard. Check auth.users INSERT count vs list 3 subscriber count for parity.

---

## Operator Notes

The onboarding automation is now complete end-to-end. Every new signup gets an email, a Telegram ping fires to Van, and the subscriber lands in Listmonk with clean segmentation attribs. The legacy 15k list and new app-era subscribers are distinguishable by source attrib — this matters for future campaigns that should not treat cold legacy subscribers the same as warm app users. The hamburger unlock was a one-character fix that unlocks blog discovery for all mobile users — highest leverage-to-effort ratio of the session. Backfilling existing accounts is the only open ops gap. Content distribution is the next revenue lever.

## Operator Take

The onboarding loop is closed — new accounts are tracked, welcomed, and segmented from day one, which means every future campaign can target by signup source, provider, and intent.
