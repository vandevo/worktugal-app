# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Stripe Configuration
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# Stripe Price IDs for Accounting Desk Consults
# Create these products in Stripe Dashboard and add the Price IDs here:
# - Tax Triage Consult: €59
# - Freelancer Start Pack: €349
# - Annual Return Consult: €149
VITE_STRIPE_PRICE_TRIAGE=price_xxx
VITE_STRIPE_PRICE_START_PACK=price_xxx
VITE_STRIPE_PRICE_ANNUAL_RETURN=price_xxx

# Make.com Webhook for Early Birds Leads (OPTIONAL)
# Create a Make.com scenario with a webhook trigger and paste the URL here
# This will be used to send confirmation emails and add leads to Airtable
# NOTE: The submit-lead Edge Function has a fallback hardcoded URL, so this is optional
MAKE_WEBHOOK_LEADS_URL=https://hook.eu2.make.com/your-webhook-id

# Make.com Webhook for Comprehensive Intake Submissions (OPTIONAL - Phase 1 MVP)
# This webhook fires after a user completes the full /accounting/intake form
# Use this to send:
# - Confirmation email to user
# - Compliance summary to admin
# - Airtable/database sync
VITE_MAKE_INTAKE_WEBHOOK_URL=https://hook.eu2.make.com/your-intake-webhook-id

# Make.com Webhook for Contact Form (REQUIRED)
# This webhook fires when users submit the /contact form
# Handles all contact purposes: partnership, accounting, info, other
# Make.com routes to appropriate handlers based on purpose field
# Integrations: Airtable, Amazon SES (user + admin emails), Telegram Bot, FluentCRM
VITE_MAKECOM_WEBHOOK_URL=https://hook.eu2.make.com/your-contact-webhook-id